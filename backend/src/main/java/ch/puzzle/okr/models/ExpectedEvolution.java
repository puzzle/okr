package ch.puzzle.okr.models;

// Do not delete the unused Enums before we don't use index based Enums in the database!
public enum ExpectedEvolution {
    INCREASE, DECREASE, CONSTANT, NONE, MIN, MAX
}
